<html>
<head><title><%= labels.brandName %> &mdash; <%= labels.closeSubAccountLinkLabel %></title></head>
<body bgcolor="#d6d3ce">
<table width="100%" cellpadding="4" cellspacing="0" border="0">
  <tr bgcolor="#003366">
    <td><font color="#ffffff" face="Arial" size="4"><b>Member Services Terminal</b></font></td>
    <td align="right"><font color="#ffffff" face="Arial" size="2">Signed on as <%= username %> &nbsp; | &nbsp; <a href="/logout"><font color="#ffffff">Sign Off</font></a></font></td>
  </tr>
</table>
<% if (labels.bannerText) { %>
  <table width="100%" cellpadding="6" cellspacing="0" border="0">
    <tr bgcolor="#e8edf5"><td><font face="Arial" size="2"><i><%= labels.bannerText %></i></font></td></tr>
  </table>
<% } %>
<br/>
<table cellpadding="2" cellspacing="0" border="0">
  <tr><td><a href="/members/<%= member.id %>">&laquo; Back to Member</a></td></tr>
</table>
<br/>
<table cellpadding="8" cellspacing="0" border="1">
  <tr>
    <td>
      <font face="Arial" size="3"><b><%= labels.closeSubAccountLinkLabel %>: <%= subAccount.id %></b></font>
      <br/><br/>
      <% if (typeof error !== "undefined" && error) { %>
        <table bgcolor="#f8d0d0" width="100%"><tr><td>
          <font color="#7a0000"><b><%= error %></b></font>
        </td></tr></table>
        <br/>
      <% } %>
      <font face="Arial" size="2">
        Member <%= member.id %> &nbsp;|&nbsp; <%= subAccount.accountType %> &nbsp;|&nbsp; $<%= subAccount.initialDeposit.toFixed(2) %>
        <br/><br/>
        Are you sure you want to close this sub-account? This cannot be undone.
      </font>
      <br/><br/>
      <form method="POST" action="/members/<%= member.id %>/sub-accounts/<%= subAccount.id %>/close">
        <input type="submit" value="<%= labels.closeSubAccountSubmitLabel %>" />
      </form>
    </td>
  </tr>
</table>
</body>
</html>
